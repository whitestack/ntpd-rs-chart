# `ntpd-rs` Docker Image and Helm Chart

Repository that builds a Docker image for
[`ntpd-rs`](https://github.com/pendulum-project/ntpd-rs) and provides a Helm
Chart for its installation in a Kubernetes cluster.
